apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodjs_app_sandbox.fullname" . }}-test-connection"
  labels:
    {{- include "nodjs_app_sandbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodjs_app_sandbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
